name: pantheon_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # 패키지 목록
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  google_fonts: ^6.3.3
  
  # 🔥 무당님 떨림 효과를 위한 애니메이션 패키지
  flutter_animate: ^4.5.2
  
  # ⚠️ 중요: 원래 쓰던 라이브러리 유지
  flutter_flip_card: ^0.0.6
  
  cached_network_image: ^3.4.1

  # 🔥 필터 3: 스마트폰 내부에 "1주일간 기억"하기 위한 저장소
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# ⭐ 플러터 설정
flutter:
  uses-material-design: true

  # 이미지 및 데이터 파일 등록 (하나씩 명시하는 게 가장 확실함!)
  assets:
    - assets/images/
    # 📌 프로필 사진들
    - assets/images/tarot.png
    - assets/images/fengshui.png
    - assets/images/shaman.png
    # 📌 배경 사진들
    - assets/images/bg_tarot.jpg
    - assets/images/bg_fengshui.jpg
    - assets/images/bg_shaman.jpg
    # 📌 기타 리소스
    - assets/images/card_back.jpg
    - assets/tier1_data.json